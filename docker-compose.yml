services:
  zimi:
    build: .
    container_name: zimi
    restart: unless-stopped
    ports:
      - "8899:8899"
    volumes:
      - ./zims:/zim:ro
    environment:
      - ZIM_DIR=/zim
      - ZIMI_MANAGE=1
